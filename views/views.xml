<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="harmony.list">
      <field name="name">Harmony list</field>
      <field name="model">harmony.harmony</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="harmony.action_window">
      <field name="name">Harmony window</field>
      <field name="res_model">harmony.harmony</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="harmony.action_server">
      <field name="name">Harmony server</field>
      <field name="model_id" ref="model_harmony_harmony"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="Harmony" id="harmony.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="harmony.menu_1" parent="harmony.menu_root"/>
    <menuitem name="Menu 2" id="harmony.menu_2" parent="harmony.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="harmony.menu_1_list" parent="harmony.menu_1"
              action="harmony.action_window"/>
    <menuitem name="Server to list" id="harmony" parent="harmony.menu_2"
              action="harmony.action_server"/>
    -->
  </data>
</odoo>